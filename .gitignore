# Rust build artefacts
/target/

# Runtime files generated by zap
agent_audit.jsonl
agent_sessions.db

# Code index DB — auto-generated cache, never commit (tracked files on Windows
# can be checked out readonly, causing "attempt to write a readonly database")
.zap/code.db
.zap/code.db-shm
.zap/code.db-wal

# User config (contains API keys — never commit)
.agent.toml
*.env
.env

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.iml

# Claude Code session files
.claude/

# Article drafts — local only
drafts/

# Development docs (tracked locally, not for distribution)
tasks.md
GAPS.md
design.md
requirements.md
VISION.md
v2.md
COMPARISON.md
.gstack/

# Demo recordings — large binaries, not for distribution
*.mp4
*.gif
*.mov
*.m4a
research/slm-coding-eval/test6-structured/project/node_modules/
