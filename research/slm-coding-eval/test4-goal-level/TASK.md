Add update support to this tasks REST API. Requirements: (1) PATCH /tasks/:id updates an existing task. The request body may contain `title` (must be a non-empty string) and/or `completed` (must be a boolean). (2) If the body contains neither a valid `title` nor a valid `completed`, or contains a `title` that is not a non-empty string, or a `completed` that is not a boolean, respond with status 400 and json { error: 'invalid body' }. (3) If no task has the given id, respond with status 404 and json { error: 'not found' }. (4) On success respond with status 200 and the updated task object as json. (5) All existing routes and behaviors must keep working exactly as before. (6) Extend test.js with assertions covering: successful completed update, successful title update, invalid body → 400, and unknown id → 404 — without breaking or reordering the existing assertions, keeping console.log('all tests passed') as the last line. (7) Run node test.js and make sure it prints 'all tests passed' before declaring done. Decide yourself which files to change and how.
