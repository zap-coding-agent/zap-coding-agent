Add a DELETE /tasks/:id endpoint to this tasks REST API. Follow these exact steps in order. Step 1 — store.js: add a new function `remove(id)` that finds the task whose `id` equals the given id, removes it from the tasks array, and returns true if a task was removed or false if no task had that id. Add `remove` to module.exports. Do not change list, get, add, or _reset. Step 2 — router.js: add handling for `DELETE /tasks/:id`. Parse the numeric id from the path using the existing idMatch (/^\/tasks\/(\d+)$/ shape). Call store.remove(Number(idMatch[1])). If it returns true, return { status: 204, json: null }. If it returns false, return { status: 404, json: { error: 'not found' } }. Keep every existing route working exactly as before. Step 3 — test.js: add NEW assertions at the END of the file. Do NOT modify or reorder the existing assertions, and keep console.log('all tests passed') as the very last line. Immediately before your new assertions call store._reset() to start from a clean state, then create exactly one task with handle('POST','/tasks',{},{title:'t'}) (it will have id 1). Then assert: (1) handle('DELETE','/tasks/1',{},null).status === 204; (2) handle('GET','/tasks/1',{},null).status === 404 (the task is gone); (3) handle('DELETE','/tasks/999',{},null).status === 404. Step 4 — run `node test.js` and confirm it prints 'all tests passed'. If it does not, fix your changes until it does.
